

const types = {
    ACTION_AUTHENTICATE: 'ACTION_AUTHENTICATE'
}

const actionAuthenticate = {
    type: types.ACTION_AUTHENTICATE,
    payload: null
};

const actions = {
    actionAuthenticate: actionAuthenticate
}

export {actions, types};