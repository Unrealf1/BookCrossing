const defaultState = {
    login: '',
    password: '',
    repeatedPassword: ''
};

const tryRegister = (state, action) => {
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }

    return state;
}
