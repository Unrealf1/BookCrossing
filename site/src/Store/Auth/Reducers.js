const defaultState = {
    login: '',
    password: ''
};

export const tryAuthenticate = (state=defaultState, action) => {
    switch (action.type) {
    
        default:
            break;
    }

    return state;
}
